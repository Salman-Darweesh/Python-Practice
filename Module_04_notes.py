import random
import statistics
import math
import os   #operating system
#import pandas as pd? 

random_number = random.randint(0,1000)  # 0 to 100 value, inclusive so it also can be 100 and not 99 
print (random_number)
result = pow(2, 3) #means 2^3 and can use positives and negatives. 
new_result = math.sqrt(4)
print (new_result)

result = pow(2, 3) #means 2^3 and can use positives and negatives. 